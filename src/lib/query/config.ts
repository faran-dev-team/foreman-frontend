/** Default cache + polling timings (ms). */
export const QUERY_STALE_MS = 30_000;
export const QUERY_GC_MS = 5 * 60_000;
/** Product spec ~15s polling for live-ish dashboard data. */
export const JOBS_POLL_MS = 15_000;
